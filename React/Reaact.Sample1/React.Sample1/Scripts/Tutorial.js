var CommentBox = React.createClass({
    render: function () {
        return className = "commentBox" >
            Hello, world;
        !I;
        am;
        a;
        CommentBox.
            < /div>;
    } });
;
ReactDOM.render(/>,, document.getElementById('content'));
